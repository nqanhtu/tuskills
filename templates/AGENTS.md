# Working with AI Agent Skills

This project uses local AI-agent skills installed under:

```txt
.agents/skills
```

When working on ZenStack-related tasks, first read:

```txt
.agents/skills/zenstack/SKILL.md
```

Then use the relevant files under `reference/`, `workflows/`, and `examples/`.
